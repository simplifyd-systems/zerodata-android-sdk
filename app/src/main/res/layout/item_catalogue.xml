<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@drawable/ic_rectangle_white"
    android:layout_margin="@dimen/one_space"
    android:padding="@dimen/two_space"
    android:layout_height="wrap_content">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/listedAppLogo"
        android:layout_width="@dimen/three_space"
        android:layout_height="@dimen/three_space"
        app:srcCompat="@drawable/ic_connect_active"
        app:layout_constraintEnd_toStartOf="@id/listedAppName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/listedAppName"
        style="@style/Theme.Simplifyd.Semi.Bold.Black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="@dimen/text_14"
        android:layout_marginStart="@dimen/two_space"
        android:layout_gravity="center_horizontal"
        app:layout_constraintStart_toEndOf="@id/listedAppLogo"
        android:text=""
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>


    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnSubmit"
        style="@style/Theme.Simplifyd.ActionWhiteButton"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/three_half_space"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:textAllCaps="false"
        android:textStyle="bold"
        app:cornerRadius="12dp"
        android:text="@string/open_app"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>